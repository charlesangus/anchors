---
phase: 15.1-additional-preferences-requirements
plan: 01
subsystem: prefs
tags: [json, persistence, round-trip, tdd]

# Dependency graph
requires:
  - phase: 15-anchor-naming
    provides: naming_regex and naming_template prefs — established the pattern this plan extends
provides:
  - naming_demo_filename preference field with default 'plate_v003.exr', type-guarded save/load
  - publish(destination_path) function — writes all prefs to caller-supplied path without touching PREFS_PATH
affects:
  - 15.1-02 (PrefsDialog UI — seeds demo filename input from naming_demo_filename, wires Publish button to publish())
  - 16-site-config (publish() is the mechanism for writing site config files)

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Type-guarded pref reads: isinstance(data.get('key'), type) before assigning module var"
    - "publish() mirrors save() dict but writes to arbitrary path — no module var mutation"
    - "dirname guard before os.makedirs: only call when dirname is non-empty (bare filename edge case)"

key-files:
  created: []
  modified:
    - prefs.py
    - tests/test_prefs.py

key-decisions:
  - "publish() guards os.path.dirname result before calling os.makedirs — bare filenames return '' which os.makedirs rejects"
  - "naming_demo_filename defaults to 'plate_v003.exr' — concrete example filename that exercises the frame-token stripping logic"

patterns-established:
  - "New pref fields follow: module-level default → globals declaration → type-guarded _load() block → save() dict entry"
  - "TestNamingDemoFilenameRoundTrip and TestPublish reuse _reload_prefs_with_temp_path helper pattern from TestNamingPrefsRoundTrip"

requirements-completed: [PREF-03, PREF-05, PREF-06]

# Metrics
duration: 2min
completed: 2026-03-17
---

# Phase 15.1 Plan 01: Prefs Backend Extensions Summary

**naming_demo_filename pref field (default 'plate_v003.exr') and publish(destination_path) function added to prefs.py with full TDD coverage — 13 tests all passing**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T04:23:46Z
- **Completed:** 2026-03-17T04:25:32Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added `naming_demo_filename = "plate_v003.exr"` module-level default with type-guarded `_load()` and `save()` persistence
- Added `publish(destination_path)` that writes all prefs to a caller-supplied path without touching PREFS_PATH or any module vars
- Added 6 new tests (3 per feature) covering defaults, round-trips, type validation, path isolation, and directory creation

## Task Commits

Each task was committed atomically:

1. **Task 1: Add naming_demo_filename to prefs.py** - `0c79406` (feat)
2. **Task 2: Add publish(path) to prefs.py** - `cc795a3` (feat)

_Note: TDD tasks — RED tests written first, GREEN implementation followed, no REFACTOR needed._

## Files Created/Modified
- `prefs.py` - Added naming_demo_filename default, extended _load() globals + type-guard, extended save() dict, added publish()
- `tests/test_prefs.py` - Added TestNamingDemoFilenameRoundTrip (3 tests) and TestPublish (3 tests)

## Decisions Made
- `publish()` guards `os.path.dirname(destination_path)` before calling `os.makedirs` — a bare filename (no directory component) returns `''` and `os.makedirs('')` raises FileNotFoundError; the guard makes the function safe for any path form.
- `naming_demo_filename` defaults to `'plate_v003.exr'` matching the plan spec — a concrete example that exercises the frame-token stripping logic downstream in `suggest_anchor_name()`.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- prefs.py now exports `naming_demo_filename` and `publish()` — the two hooks needed by Phase 15.1 Plan 02 (PrefsDialog UI)
- Plan 02 can seed the demo filename input from `prefs.naming_demo_filename` and wire the Publish button to `prefs_module.publish()`
- No blockers.

## Self-Check: PASSED

- prefs.py: FOUND
- tests/test_prefs.py: FOUND
- 15.1-01-SUMMARY.md: FOUND
- Commit 0c79406: FOUND
- Commit cc795a3: FOUND
- All 13 tests: PASSED

---
*Phase: 15.1-additional-preferences-requirements*
*Completed: 2026-03-17*
