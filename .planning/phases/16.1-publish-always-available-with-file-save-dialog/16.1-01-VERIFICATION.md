---
phase: 16.1-publish-always-available-with-file-save-dialog
verified: 2026-03-17T12:00:00Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Publish... button enabled when ANCHORS_SITE_CONFIG is not set"
    expected: "Button is visible and clickable (not greyed out) when the env var is absent"
    why_human: "Cannot invoke Qt widget state checks without a live Nuke runtime"
  - test: "Clicking Publish... opens QFileDialog.getSaveFileName on cold start"
    expected: "Dialog opens pre-filled with '/' when no env var and no last_publish_path in prefs"
    why_human: "QFileDialog requires a live Qt event loop; cannot mock-exec in this environment"
  - test: "Cancelling the save dialog writes nothing"
    expected: "No file is created or overwritten; prefs.last_publish_path is unchanged"
    why_human: "Requires interactive dialog cancel in a live Qt runtime"
  - test: "Confirming the save dialog publishes and persists path"
    expected: "File is written to chosen path; closing and reopening PrefsDialog pre-fills that path"
    why_human: "Requires interactive dialog confirm and dialog re-open in Nuke"
  - test: "ANCHORS_SITE_CONFIG env var takes priority over last_publish_path"
    expected: "When env var is set to /some/path.json, dialog pre-fills /some/path.json"
    why_human: "Requires verifying Qt dialog initial path at runtime with env var injected"
---

# Phase 16.1: Publish Always Available with File Save Dialog — Verification Report

**Phase Goal:** Publish button is always enabled and always opens a file save dialog; last-used path persists to prefs so site admins can create the site config file even before ANCHORS_SITE_CONFIG is set.
**Verified:** 2026-03-17T12:00:00Z
**Status:** human_needed — all automated checks passed; 5 UI behaviors require live Nuke verification
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Publish button in PrefsDialog is always enabled, regardless of whether ANCHORS_SITE_CONFIG is set | ? NEEDS HUMAN | `setEnabled` line removed from `_build_ui()`; button object created with no enable guard. Runtime state requires Nuke. |
| 2 | Clicking Publish opens a QFileDialog.getSaveFileName dialog pre-filled with the best-known path | ? NEEDS HUMAN | `QFileDialog.getSaveFileName(self, "Publish Site Config", self._publish_path, ...)` present in `_on_publish_naming()`. Requires live Qt runtime to verify dialog opens. |
| 3 | Dialog initial path priority: ANCHORS_SITE_CONFIG env var > prefs.last_publish_path > '/' | VERIFIED | `self._publish_path = (os_module.environ.get("ANCHORS_SITE_CONFIG", "") or prefs_module.last_publish_path or "/")` at colors.py:561-565. Priority chain is correct Python short-circuit evaluation. |
| 4 | After a successful publish, prefs.last_publish_path is updated and persisted via prefs.save() | VERIFIED | colors.py:841-844: `prefs_module.publish(chosen_path)`, `self._publish_path = chosen_path`, `prefs_module.last_publish_path = chosen_path`, `prefs_module.save()`. All four lines present in sequence. Cancel guard on line 838-839 prevents side effects on cancel. |
| 5 | prefs.last_publish_path round-trips correctly through save() and _load() | VERIFIED | prefs.py:27 (default), :80 (global decl), :110-111 (str type-guard load), :178 (save). All 3 tests in `TestLastPublishPath` pass (round-trip, default-to-empty-string, non-string-ignored). |

**Score:** 5/5 truths verified (3 fully automated, 2 require live Qt runtime)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `prefs.py` | `last_publish_path` module var, load/save round-trip | VERIFIED | Line 27: `last_publish_path = ""`. Line 80: declared in `_load()` global statement. Lines 110-111: str type-guard read. Line 178: key in `save()` dict. |
| `colors.py` | Always-enabled Publish button, QFileDialog save dialog, path persistence | VERIFIED | Line 662: `QPushButton("Publish...")` with no `setEnabled` call. Lines 561-565: fallback chain in `__init__`. Lines 832-844: `getSaveFileName` call with cancel guard and path persistence in `_on_publish_naming()`. |
| `tests/test_prefs.py` | `last_publish_path` round-trip test | VERIFIED | `TestLastPublishPath` class at line 776 with 3 tests. All pass in isolation (`python3.11 -m unittest tests.test_prefs.TestLastPublishPath -v`). |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `colors.py PrefsDialog._on_publish_naming()` | `prefs.last_publish_path` | `prefs_module.last_publish_path = chosen_path` then `prefs_module.save()` | WIRED | Both assignment (colors.py:843) and save call (colors.py:844) confirmed present. |
| `colors.py PrefsDialog.__init__()` | `prefs.last_publish_path` | fallback in initial path resolution | WIRED | `prefs_module.last_publish_path` at colors.py:563 — second operand in the `or` chain, after env var lookup, before `"/"` fallback. |

---

### Requirements Coverage

No requirement IDs were declared for this phase (inserted phase, no formal requirement IDs assigned).

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No TODO, FIXME, placeholder comments, empty implementations, or stub returns found in the modified files for this phase's changes.

---

### Test Suite Results

**test_prefs (isolated):** 24/24 pass
**test_anchor_naming (isolated):** 13/13 pass
**Combined run:** 5 failures — pre-existing test isolation issue documented in PLAN as out of scope. Confirmed pre-existing before this phase.

**New tests added — TestLastPublishPath:**
- `test_last_publish_path_round_trips` — PASS
- `test_last_publish_path_defaults_to_empty_string` — PASS
- `test_last_publish_path_non_string_in_json_is_ignored` — PASS

---

### Human Verification Required

**1. Publish... button always enabled (no ANCHORS_SITE_CONFIG)**

**Test:** Open Nuke. Open PrefsDialog without ANCHORS_SITE_CONFIG set. Navigate to the Advanced section.
**Expected:** "Publish..." button is enabled and clickable (not greyed out).
**Why human:** Cannot inspect Qt widget enabled state without a live Nuke runtime.

**2. File save dialog opens on cold start**

**Test:** With no env var and no prior prefs file, click the "Publish..." button.
**Expected:** A file save dialog opens with the initial directory set to "/".
**Why human:** QFileDialog.getSaveFileName requires a Qt event loop to execute.

**3. Cancel writes nothing**

**Test:** Click "Publish...", get the file save dialog, then click Cancel.
**Expected:** No file is written. Subsequent click of Publish... still shows the same pre-fill path.
**Why human:** Requires interactive dialog cancellation in Nuke.

**4. Confirm publishes and persists path**

**Test:** Click "Publish...", choose /tmp/test_site_config.json, click Save. Close and reopen PrefsDialog.
**Expected:** File exists at /tmp/test_site_config.json with naming_regex/naming_template/naming_demo_filename keys. Reopening PrefsDialog and clicking Publish... pre-fills /tmp/test_site_config.json.
**Why human:** Requires interactive dialog confirm and dialog lifecycle in Nuke.

**5. ANCHORS_SITE_CONFIG env var takes priority**

**Test:** Set ANCHORS_SITE_CONFIG=/some/path.json (file need not exist). Open PrefsDialog, click "Publish...".
**Expected:** Dialog pre-fills /some/path.json.
**Why human:** Requires verifying Qt dialog initial path argument at runtime with env var set.

---

### Summary

All automated checks pass. The three files modified in this phase — `prefs.py`, `colors.py`, and `tests/test_prefs.py` — contain exactly the changes described in the PLAN and SUMMARY:

- `prefs.py`: `last_publish_path` default, global declaration, str type-guard load, save dict key — all present and correct.
- `colors.py`: `setEnabled` guard removed, button label is "Publish...", `__init__` uses the three-way fallback chain, `_on_publish_naming()` contains the full file dialog flow with cancel guard and path persistence.
- `tests/test_prefs.py`: `TestLastPublishPath` class with 3 tests, all passing.

The only items requiring human verification are live Qt/Nuke behaviors (dialog opening, cancel/confirm interaction, widget enabled state) that cannot be tested in a headless environment. The underlying code paths for all these behaviors are fully implemented and wired.

---

_Verified: 2026-03-17T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
