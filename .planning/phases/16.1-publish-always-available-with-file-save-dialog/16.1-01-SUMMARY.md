---
phase: 16.1-publish-always-available-with-file-save-dialog
plan: 01
subsystem: ui
tags: [prefs, publish, QFileDialog, site-config, preferences-dialog]

# Dependency graph
requires:
  - phase: 16-site-config
    provides: prefs.publish(), ANCHORS_SITE_CONFIG env var, PrefsDialog Publish button
  - phase: 15.1-additional-preferences-requirements
    provides: PrefsDialog naming fields, _on_publish_naming() structure
provides:
  - prefs.last_publish_path module var with load/save round-trip
  - PrefsDialog Publish... button always enabled (no setEnabled guard)
  - QFileDialog.getSaveFileName flow in _on_publish_naming()
  - Path persistence: chosen path saved to prefs.last_publish_path after successful publish
affects:
  - any future phase using PrefsDialog publish flow
  - site admins creating first-time site config files

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "path-priority chain: env var > last_publish_path > '/' for dialog pre-fill"
    - "str type-guard for new prefs fields in _load(): if isinstance(data.get('field'), str)"
    - "publish path persistence: set prefs_module.last_publish_path then call prefs_module.save()"

key-files:
  created: []
  modified:
    - prefs.py
    - colors.py
    - tests/test_prefs.py

key-decisions:
  - "16.1-01: _publish_path fallback chain: ANCHORS_SITE_CONFIG or prefs_module.last_publish_path or '/' — allows cold-start without env var"
  - "16.1-01: last_publish_path default is '' (not '/') at module level — '/' only applied in PrefsDialog.__init__() as UI default"
  - "16.1-01: auto_advance=true — Task 3 human-verify checkpoint auto-approved; no UI runtime available in this environment"

patterns-established:
  - "PrefsDialog path pre-fill: env var > persisted last path > fallback"
  - "Cancel guard in publish flow: if not chosen_path: return — no side effects on cancel"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-17
---

# Phase 16.1 Plan 01: Publish Always Available with File Save Dialog Summary

**Always-enabled Publish... button in PrefsDialog opens QFileDialog pre-filled with ANCHORS_SITE_CONFIG > last_publish_path > /, persisting chosen path to prefs after successful publish**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T11:38:33Z
- **Completed:** 2026-03-17T11:40:15Z
- **Tasks:** 2 executed + 1 auto-approved checkpoint
- **Files modified:** 3

## Accomplishments
- Added `last_publish_path = ""` to prefs.py with full load/save round-trip (str type guard, global declaration)
- Removed `setEnabled(bool(self._publish_path))` guard so Publish... button is always enabled
- Replaced direct `prefs_module.publish(self._publish_path)` call with `QFileDialog.getSaveFileName` flow including cancel guard and path persistence

## Task Commits

Each task was committed atomically:

1. **Task 1 RED: add failing tests for last_publish_path** - `9cda434` (test)
2. **Task 1 GREEN: add last_publish_path to prefs with load/save round-trip** - `314de66` (feat)
3. **Task 2: always-enabled Publish button with file save dialog** - `4fb36ea` (feat)
4. **Task 3: human-verify** - auto-approved (auto_advance=true)

**Plan metadata:** (docs commit — see below)

_Note: TDD tasks have separate RED (test) and GREEN (feat) commits_

## Files Created/Modified
- `/workspace/prefs.py` - Added `last_publish_path` default, global in `_load()`, type-guard read, and key in `save()` dict
- `/workspace/colors.py` - Updated `_publish_path` init with fallback chain, removed `setEnabled` guard, changed button label to "Publish...", replaced `_on_publish_naming()` with file dialog flow
- `/workspace/tests/test_prefs.py` - Added `TestLastPublishPath` class with 3 tests: round-trip, default-to-empty-string, non-string-ignored

## Decisions Made
- `_publish_path` fallback chain: `ANCHORS_SITE_CONFIG or last_publish_path or "/"` — ensures a sensible default even on cold start with no env var set
- `last_publish_path` module-level default is `""` (empty string); the `"/"` fallback is only applied in `PrefsDialog.__init__()` as a UI-only convenience default
- `prefs_module` import in `__init__()` was already present at line 545 — no duplicate import needed for the fallback chain access

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Publish button is now fully usable without ANCHORS_SITE_CONFIG configured
- Site admins can create a site config file for the first time using the dialog
- Human verification in Nuke still required to confirm QFileDialog behavior (Task 3 checkpoint was auto-approved in headless environment)

---
*Phase: 16.1-publish-always-available-with-file-save-dialog*
*Completed: 2026-03-17*
