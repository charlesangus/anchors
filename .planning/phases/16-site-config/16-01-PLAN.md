---
phase: 16-site-config
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_prefs.py
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03

must_haves:
  truths:
    - "Failing tests exist for site config loading (SITE-01)"
    - "Failing tests exist for field locking and publish naming-only behavior (SITE-02)"
    - "Failing tests exist for override flag persistence and round-trip (SITE-03)"
    - "test_publish_writes_to_given_path updated to expect ONLY naming keys"
    - "All new tests fail (RED) before prefs.py changes — confirms tests are meaningful"
  artifacts:
    - path: "tests/test_prefs.py"
      provides: "TestSiteConfigLoading class + updated TestPublish.test_publish_writes_to_given_path"
      contains: "TestSiteConfigLoading"
  key_links:
    - from: "tests/test_prefs.py"
      to: "prefs._site_config"
      via: "prefs_module._site_config after reload"
      pattern: "_site_config"
    - from: "tests/test_prefs.py"
      to: "prefs._user_naming_regex"
      via: "shadow var isolation test"
      pattern: "_user_naming_regex"
---

<objective>
Write failing tests (Wave 0 TDD scaffold) for all site config behaviors before any production code changes.

Purpose: Establish the RED baseline so plan 02 (prefs.py backend) produces tests turning GREEN — not tests written to pass already-written code.
Output: New `TestSiteConfigLoading` class in `tests/test_prefs.py` + updated `TestPublish.test_publish_writes_to_given_path`.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-site-config/16-CONTEXT.md
@.planning/phases/16-site-config/16-RESEARCH.md

<interfaces>
<!-- Key patterns extracted from tests/test_prefs.py and prefs.py for executor reference -->

From tests/test_prefs.py — _reload_prefs_with_temp_path helper (existing, use verbatim):
```python
def _reload_prefs_with_temp_path(self, temp_prefs_path):
    import sys
    if 'prefs' in sys.modules:
        del sys.modules['prefs']
    import constants
    constants.PREFS_PATH = temp_prefs_path
    import prefs
    return prefs
```

From prefs.py — current module-level vars (before Phase 16 changes):
```python
plugin_enabled = True
link_classes_paste_mode = "create_link"
custom_colors = []
naming_regex = ""
naming_template = ""
naming_demo_filename = "plate_v003.exr"
```

From prefs.py — current publish() signature:
```python
def publish(destination_path):
    """Write current preference values to destination_path..."""
    # Currently writes: plugin_enabled, link_classes_paste_mode, custom_colors,
    # naming_regex, naming_template, naming_demo_filename
    # Phase 16 MUST restrict this to naming fields ONLY
```

From tests/test_prefs.py — TestPublish._reload_prefs_with_temp_path usage:
The TestPublish class uses the same helper. It must be updated so
test_publish_writes_to_given_path asserts ONLY naming keys are present
and that plugin_enabled / link_classes_paste_mode / custom_colors are ABSENT.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write TestSiteConfigLoading test class (RED)</name>
  <files>tests/test_prefs.py</files>
  <behavior>
    - test_site_config_values_applied_as_effective_values: Write a temp site config JSON with naming_regex="(?P<shot>.+)_v\d+", load prefs with ANCHORS_SITE_CONFIG pointing to it, assert prefs_module.naming_regex equals the site config value (not the user prefs value)
    - test_site_config_missing_env_var_is_silent_noop: Load prefs with ANCHORS_SITE_CONFIG unset, assert naming_regex equals user prefs value with no exception raised
    - test_site_config_corrupt_file_is_silent_noop: Write a corrupt JSON file, load prefs with ANCHORS_SITE_CONFIG pointing to it, assert naming_regex equals user prefs value with no exception raised
    - test_site_config_missing_file_path_is_silent_noop: Set ANCHORS_SITE_CONFIG to a path that does not exist, load prefs, assert naming_regex equals user prefs value with no exception raised
    - test_site_config_locks_fields_user_values_preserved_in_shadow_vars: Write site config with naming_regex="admin_regex", write user prefs with naming_regex="user_regex", load — assert prefs_module.naming_regex == "admin_regex" (effective) AND prefs_module._user_naming_regex == "user_regex" (shadow)
    - test_site_config_override_true_uses_user_values: Same setup as above but user prefs has site_config_override=True — assert prefs_module.naming_regex == "user_regex" (override wins)
    - test_site_config_override_round_trip: Write user prefs with site_config_override=True, reload, assert prefs_module.site_config_override is True; call save(), reload again, assert still True
    - test_site_config_absent_fields_not_locked: Write site config with ONLY naming_regex (not naming_template), load — assert naming_regex == site config value, assert naming_template == user prefs value
  </behavior>
  <action>
    Append a new test class `TestSiteConfigLoading` to tests/test_prefs.py after `TestPublish`.

    Each test method uses `_reload_prefs_with_temp_path` (the existing helper on self). For tests involving ANCHORS_SITE_CONFIG, patch `os.environ['ANCHORS_SITE_CONFIG']` via `unittest.mock.patch.dict(os.environ, {...})` or set it directly in setUp/tearDown.

    The helper deletes and re-imports prefs — this means ANCHORS_SITE_CONFIG must be set in the environment BEFORE the reload so `_load()` reads it. Set the env var before calling `_reload_prefs_with_temp_path`, clear it in tearDown.

    These tests MUST fail (RED) before prefs.py changes. Attributes `_site_config`, `_user_naming_regex`, `_user_naming_template`, `_user_naming_demo_filename`, `site_config_override` do not exist yet on the prefs module.

    Run after writing to confirm RED: `python -m pytest tests/test_prefs.py -k "site_config or override_round_trip" -x -q`
  </action>
  <verify>
    <automated>python -m pytest tests/test_prefs.py -k "site_config or override_round_trip" -x -q 2>&1 | grep -E "FAILED|ERROR|error"</automated>
  </verify>
  <done>All new TestSiteConfigLoading test methods exist and FAIL (RED) with AttributeError or similar — confirming they test things not yet implemented</done>
</task>

<task type="tdd">
  <name>Task 2: Update TestPublish.test_publish_writes_to_given_path (RED)</name>
  <files>tests/test_prefs.py</files>
  <behavior>
    - Updated test asserts ONLY naming keys present in published JSON: naming_regex, naming_template, naming_demo_filename
    - Updated test asserts plugin_enabled, link_classes_paste_mode, custom_colors are ABSENT from published JSON (not just wrong values — the keys must not be in the output at all)
    - Test must FAIL (RED) against current publish() which writes all prefs fields
  </behavior>
  <action>
    Modify the existing `TestPublish.test_publish_writes_to_given_path` method in tests/test_prefs.py:

    1. Remove assertions for `plugin_enabled`, `link_classes_paste_mode`, `custom_colors`
    2. Add negative assertions: `assertNotIn('plugin_enabled', data)`, `assertNotIn('link_classes_paste_mode', data)`, `assertNotIn('custom_colors', data)`
    3. Keep the positive assertions for the three naming fields
    4. Keep the `prefs_module.naming_regex`, `naming_template`, `naming_demo_filename` value assignments

    Also remove the lines that set `prefs_module.plugin_enabled = False`, `prefs_module.link_classes_paste_mode = 'passthrough'`, `prefs_module.custom_colors = [0x112233ff]` from the test since these are no longer relevant to publish output.

    Run to confirm RED: `python -m pytest tests/test_prefs.py -k "test_publish_writes_to_given_path" -x -q`
  </action>
  <verify>
    <automated>python -m pytest tests/test_prefs.py -k "test_publish_writes_to_given_path" -x -q 2>&1 | grep -E "FAILED|passed"</automated>
  </verify>
  <done>test_publish_writes_to_given_path FAILS (RED) because current publish() includes plugin_enabled/link_classes_paste_mode/custom_colors — the assertNotIn assertions fire</done>
</task>

</tasks>

<verification>
Run full prefs test suite to baseline before plan 02: `python -m pytest tests/test_prefs.py -x -q`

Expected: pre-existing tests still pass, new site_config and updated publish tests fail (RED). The count of failures is the target for plan 02 to turn GREEN.
</verification>

<success_criteria>
- TestSiteConfigLoading class exists with 8 test methods all in FAILED/ERROR state
- test_publish_writes_to_given_path updated and FAILING (RED) due to assertNotIn checks
- All other existing prefs tests still GREEN
- `python -m pytest tests/test_prefs.py -x -q` shows the new failures, existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/16-site-config/16-01-SUMMARY.md`
</output>
