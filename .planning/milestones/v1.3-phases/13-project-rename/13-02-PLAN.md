---
phase: 13-project-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - tests/test_cross_script_paste.py
  - tests/test_dot_type_distinction.py
  - tests/test_anchor_color_system.py
  - tests/test_prefs.py
  - tests/test_dot_anchor_name_sync.py
  - .github/workflows/release.yml
  - README.md
autonomous: false
requirements:
  - REN-01
  - REN-02

must_haves:
  truths:
    - "pytest tests/ passes in full with zero failures or errors"
    - "release.yml builds an anchors/ folder and produces anchors-{version}.zip"
    - "README.md installation instructions reference the anchors folder and anchors_prefs filename"
    - "README.md Python API section documents migrate_script() under the anchors module"
    - "GitHub repository is accessible at the anchors URL"
  artifacts:
    - path: "tests/test_cross_script_paste.py"
      provides: "Updated patch paths for anchors module"
      contains: "from anchors import"
    - path: "tests/test_dot_type_distinction.py"
      provides: "Updated patch paths for anchors module"
      contains: "patch('anchors."
    - path: "tests/test_anchor_color_system.py"
      provides: "Updated string checks for import anchors pattern"
      contains: "anchors"
    - path: "tests/test_prefs.py"
      provides: "Updated file path strings for anchors_prefs.json"
      contains: "anchors_prefs.json"
    - path: ".github/workflows/release.yml"
      provides: "CI that builds anchors/ folder and anchors-{version}.zip"
      contains: "mkdir anchors"
    - path: "README.md"
      provides: "Updated installation and API documentation"
      contains: "anchors folder"
  key_links:
    - from: "tests/test_cross_script_paste.py"
      to: "anchors module"
      via: "from anchors import / patch('anchors.*')"
      pattern: "from anchors import|patch\\('anchors\\."
    - from: ".github/workflows/release.yml"
      to: "anchors.py"
      via: "cp ... anchors.py ... anchors/"
      pattern: "anchors\\.py.*anchors/"
---

<objective>
Update all test files, the CI workflow, and README.md to use the renamed anchors module. Then rename the GitHub repository from paste_hidden to anchors.

Purpose: Completes REN-01 by making tests and CI consistent with the source rename. Satisfies REN-02 by renaming the remote repo.
Output: All 6 test files updated, release.yml updated, README.md updated, GitHub repo renamed.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-project-rename/13-CONTEXT.md
@.planning/phases/13-project-rename/13-01-SUMMARY.md
</context>

<interfaces>
<!-- Test files that need updating — key patterns. -->

Current pattern in test files (all must change):
  - `from paste_hidden import paste_hidden` → `from anchors import paste_anchors`
  - `from paste_hidden import copy_hidden` → `from anchors import copy_anchors`
  - `from paste_hidden import _extract_display_name_from_fqnn` → `from anchors import _extract_display_name_from_fqnn`
  - `patch('paste_hidden.nuke')` → `patch('anchors.nuke')`
  - `patch('paste_hidden.nukescripts')` → `patch('anchors.nukescripts')`
  - `patch('paste_hidden.is_link', ...)` → `patch('anchors.is_link', ...)`
  - `patch('paste_hidden.is_anchor', ...)` → `patch('anchors.is_anchor', ...)`
  - `patch('paste_hidden.find_anchor_node', ...)` → `patch('anchors.find_anchor_node', ...)`
  - `patch('paste_hidden.find_anchor_by_name', ...)` → `patch('anchors.find_anchor_by_name', ...)`
  - `patch('paste_hidden.setup_link_node', ...)` → `patch('anchors.setup_link_node', ...)`
  - `patch('paste_hidden.add_input_knob', ...)` → `patch('anchors.add_input_knob', ...)`
  - `patch('paste_hidden.get_fully_qualified_node_name', ...)` → `patch('anchors.get_fully_qualified_node_name', ...)`
  - All `paste_hidden()` function call sites → `paste_anchors()`
  - All `copy_hidden()` function call sites → `copy_anchors()`

In test_prefs.py — file path strings:
  - `'paste_hidden_prefs.json'` → `'anchors_prefs.json'`
  - `'paste_hidden_user_palette.json'` stays unchanged (legacy palette file name is still paste_hidden)

In test_anchor_color_system.py — string pattern checks:
  - `'import paste_hidden.menu'` check → `'import anchors.menu'`
  - Any docstring references to "paste_hidden" module

Current release.yml build step (must change):
  mkdir paste_hidden
  cp anchor.py colors.py constants.py labels.py link.py \
     menu.py paste_hidden.py prefs.py tabtabtab.py util.py \
     README.md LICENSE \
     paste_hidden/
  zip -r "paste_hidden-${GITHUB_REF_NAME}.zip" paste_hidden/
  files: paste_hidden-${{ github.ref_name }}.zip

New release.yml build step:
  mkdir anchors
  cp anchor.py anchors.py colors.py constants.py labels.py link.py \
     menu.py prefs.py tabtabtab.py util.py \
     README.md LICENSE \
     anchors/
  zip -r "anchors-${GITHUB_REF_NAME}.zip" anchors/
  files: anchors-${{ github.ref_name }}.zip
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update all test files and CI workflow for renamed module</name>
  <files>tests/test_cross_script_paste.py, tests/test_dot_type_distinction.py, tests/test_anchor_color_system.py, tests/test_prefs.py, tests/test_dot_anchor_name_sync.py, .github/workflows/release.yml</files>
  <action>
    **tests/test_cross_script_paste.py** — apply these replacements throughout the file:
    - `from paste_hidden import _extract_display_name_from_fqnn` → `from anchors import _extract_display_name_from_fqnn`
    - `from paste_hidden import paste_hidden` → `from anchors import paste_anchors`
    - All `paste_hidden()` calls (the function, not the module) → `paste_anchors()`
    - All `patch('paste_hidden.` → `patch('anchors.`
    - Module docstring references to `paste_hidden()` function → `paste_anchors()`
    - Comment references like `paste_hidden() Path A/C` → `paste_anchors() Path A/C`
    - Docstring line: `- paste_hidden() Path A/C cross-script name-based reconnect` → `- paste_anchors() Path A/C cross-script name-based reconnect`
    - Docstring line: `- paste_hidden() Path B Dot cross-script disconnection` → `- paste_anchors() Path B Dot cross-script disconnection`

    **tests/test_dot_type_distinction.py** — apply these replacements throughout:
    - `from paste_hidden import paste_hidden` → `from anchors import paste_anchors`
    - `from paste_hidden import copy_hidden` → `from anchors import copy_anchors`
    - All `paste_hidden()` calls → `paste_anchors()`
    - All `copy_hidden()` calls → `copy_anchors()`
    - All `patch('paste_hidden.` → `patch('anchors.`
    - Module docstring / comment references to `copy_hidden()` / `paste_hidden()` → `copy_anchors()` / `paste_anchors()`

    **tests/test_anchor_color_system.py** — search for all `paste_hidden` references:
    - Any `'import paste_hidden.menu'` string literals used in assertions → `'import anchors.menu'`
    - Docstring/comment text referencing `paste_hidden` as a module name → update to `anchors`
    - Look for the regex `re.compile(r'import paste_hidden\.menu')` and update to `re.compile(r'import anchors\.menu')`
    - Any `assertNotIn('import paste_hidden.menu', ...)` → `assertNotIn('import anchors.menu', ...)`

    **tests/test_prefs.py** — update file path strings:
    - All `'paste_hidden_prefs.json'` → `'anchors_prefs.json'`
    - Keep `'paste_hidden_user_palette.json'` unchanged — the legacy palette file retains its original name in tests because the real file on disk is not renamed (prefs.py reads it as the old palette source)
    - Update module docstring: `"""Tests for prefs.py — preferences singleton for paste_hidden."""` → `"""Tests for prefs.py — preferences singleton for the anchors plugin."""`

    **tests/test_dot_anchor_name_sync.py** — scan for any `paste_hidden` references:
    - Comments mentioning `copy_hidden` → update to `copy_anchors`
    - Any patch paths `'paste_hidden.*'` → `'anchors.*'`
    - Read the file first and apply only what is actually there

    **.github/workflows/release.yml** — update the "Build release ZIP" step:

    Replace the entire `run: |` block in the "Build release ZIP" step with:
    ```yaml
          run: |
            mkdir anchors
            cp anchor.py anchors.py colors.py constants.py labels.py link.py \
               menu.py prefs.py tabtabtab.py util.py \
               README.md LICENSE \
               anchors/
            zip -r "anchors-${GITHUB_REF_NAME}.zip" anchors/
    ```

    Also update the "Publish GitHub Release" step's `files:` line:
    ```yaml
              files: anchors-${{ github.ref_name }}.zip
    ```

    The release `name:` field stays as `${{ github.ref_name }}` (unchanged).
  </action>
  <verify>
    <automated>cd /workspace && python3 -m pytest tests/ -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>pytest tests/ reports 0 failures, 0 errors. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md for new module name, install path, and migrate_script() docs</name>
  <files>README.md</files>
  <action>
    Update README.md with the following changes. Preserve all existing content structure; only update what references paste_hidden:

    **Title and installation section:**
    - Title `# Paste Hidden` → `# Anchors`
    - Installation paragraph: `Copy the \`paste_hidden\` folder into your \`~/.nuke\` folder` → `Copy the \`anchors\` folder into your \`~/.nuke\` folder`
    - `nuke.pluginAddPath('paste_hidden')` → `nuke.pluginAddPath('anchors')`
    - `The included \`menu.py\` will handle replacing the built-in copy/paste functions.` — unchanged

    **Copy / Paste section:**
    - The `## Copy / Paste (\`import paste_hidden\`)` heading → `## Copy / Paste (\`import anchors\`)`
    - Function signatures in the code block:
      ```python
      anchors.copy_anchors()
      anchors.cut_anchors()
      anchors.paste_anchors() -> nuke.Node   # returns last pasted node, same as nuke.nodePaste()
      anchors.paste_multiple_anchors()
      ```
    - Paragraph text: `These are drop-in replacements for Nuke's built-in copy/cut/paste. They are wired to \`Ctrl+C/X/V\` by \`menu.py\` automatically on installation. You only need to call them directly if you are building your own menu or keybind setup.` — keep unchanged.
    - Old-style equivalents line: `copy_old()`, `cut_old()`, and `paste_old()` — names unchanged (still these names), just note they're available as `anchors.copy_old()` etc.

    **Configuration section:**
    - `Edit \`constants.py\`` line — unchanged (still constants.py)

    **Python API section:**
    - `The following functions are stable entry points for tools and scripts that want to integrate with paste_hidden programmatically.` → `The following functions are stable entry points for tools and scripts that want to integrate with anchors programmatically.`

    **Add migrate_script() documentation** — add a new `## Migration` section near the bottom of the README (after `## Copy / Paste` section or before `# Label Utilities`), OR add a `### migrate_script()` subsection within the `## Copy / Paste` API section. Place it at the bottom of the `## Copy / Paste (\`import anchors\`)` API section:

    ```markdown
    ### Migration from paste_hidden

    If you have existing Nuke scripts (.nk files) created with the old `paste_hidden` plugin,
    run the following once per script to migrate the hidden knob names to the new `anchors` format:

    ```python
    import anchors
    anchors.migrate_script()
    ```

    This renames the `paste_hidden_dot_anchor` and `paste_hidden_dot_type` knobs on all nodes in
    the current script to `anchors_dot_anchor` and `anchors_dot_type`. It prints a summary of
    nodes and knobs updated so you can verify the migration. The old knobs are removed after
    migration. This operation is not undoable — save a backup of your script first.
    ```

    Keep all other README content unchanged.
  </action>
  <verify>
    <automated>grep -n "anchors folder\|pluginAddPath('anchors')\|migrate_script\|import anchors" /workspace/README.md | head -20</automated>
  </verify>
  <done>README.md references anchors folder for installation, has migrate_script() documentation, and Copy/Paste API section uses anchors.copy_anchors / anchors.paste_anchors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete rename — tests pass and no stale references</name>
  <action>Human-verify task: check that all tests pass and no stale paste_hidden references remain.</action>
  <what-built>
    Plan 01 renamed all source files and Plan 02 updated tests, CI, and README. The full test suite should be passing. The rename is complete in the local repo.
  </what-built>
  <how-to-verify>
    1. Run `cd /workspace && python3 -m pytest tests/ -q` — all tests should pass with 0 failures
    2. Run `grep -r "paste_hidden" /workspace --include="*.py" --include="*.yml" --include="*.md" | grep -v ".planning" | grep -v "__pycache__"` — should return ONLY:
       - Legitimate old-name references inside migrate_script() (the OLD_DOT_ANCHOR / OLD_DOT_TYPE literals)
       - The legacy palette path USER_PALETTE_PATH (`paste_hidden_user_palette.json`) in constants.py — intentionally preserved
       - Migration docstrings/comments referencing the old name for documentation purposes
       - Any .nk file references (not expected in this repo)
    3. Confirm release.yml builds `anchors/` folder and produces `anchors-{version}.zip`
    4. Confirm README.md installation shows `anchors` folder and `pluginAddPath('anchors')`
  </how-to-verify>
  <resume-signal>Type "approved" when tests pass and grep shows only legitimate paste_hidden references, or describe any issues found.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Rename GitHub repository to anchors (REN-02)</name>
  <action>Rename the GitHub repository from paste_hidden to anchors</action>
  <instructions>
    Run this command to rename the GitHub repo (requires gh CLI authenticated):

    ```
    gh repo rename anchors
    ```

    If your local remote URL still points to the old name after renaming, GitHub will redirect automatically, but you may want to update it:

    ```
    git remote set-url origin https://github.com/YOUR_USERNAME/anchors.git
    ```

    Verify: `gh repo view` should show the repo name as `anchors`.
  </instructions>
  <resume-signal>Type "renamed" when the GitHub repo is accessible at the anchors URL, or "skip" if this is deferred.</resume-signal>
</task>

</tasks>

<verification>
Full verification sequence after both tasks complete:
```bash
cd /workspace && python3 -m pytest tests/ -q
```
Expected: all tests pass, 0 failures, 0 errors.

```bash
grep -r "paste_hidden" /workspace --include="*.py" --include="*.yml" --include="*.md" | grep -v ".planning" | grep -v "__pycache__"
```
Expected: only the intentional legacy references (USER_PALETTE_PATH, migrate_script OLD_ literals, migration documentation).
</verification>

<success_criteria>
- pytest tests/ passes 100%
- No unintended paste_hidden references remain in Python source, tests, CI, or README
- release.yml produces anchors-{version}.zip from an anchors/ build directory
- README.md documents the anchors install path and migrate_script()
- GitHub repo renamed to anchors (REN-02)
</success_criteria>

<output>
After completion, create `.planning/phases/13-project-rename/13-02-SUMMARY.md`
</output>
