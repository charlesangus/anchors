---
phase: quick-260328-6mn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - anchor.py
  - menu.py
  - tests/test_anchor_navigation.py
autonomous: true
requirements: [QUICK-260328-6mn]
must_haves:
  truths:
    - "User selects an anchor node and invokes command — DAG jumps to that anchor's upstream tree"
    - "Alt+Z (navigate_back) returns to the position before the jump"
    - "Command is a no-op when no anchor is selected or plugin is disabled"
  artifacts:
    - path: "anchor.py"
      provides: "jump_to_selected_anchor() function"
      contains: "def jump_to_selected_anchor"
    - path: "menu.py"
      provides: "Menu entry with keyboard shortcut for jump-to-anchor"
      contains: "jump_to_selected_anchor"
    - path: "tests/test_anchor_navigation.py"
      provides: "Tests for jump_to_selected_anchor behavior"
      contains: "jump_to_selected_anchor"
  key_links:
    - from: "anchor.py:jump_to_selected_anchor"
      to: "anchor.py:_save_dag_position"
      via: "direct call before navigate"
      pattern: "_save_dag_position"
    - from: "anchor.py:jump_to_selected_anchor"
      to: "anchor.py:navigate_to_anchor"
      via: "direct call with selected anchor node"
      pattern: "navigate_to_anchor"
---

<objective>
Add a "jump to anchor" command that navigates to the selected anchor node's upstream tree without showing a picker popup. Saves the current DAG position first so Alt+Z (navigate_back) returns.

Purpose: Provides a faster navigation path when the user already has the target anchor selected — skips the tabtabtab picker entirely.
Output: New `jump_to_selected_anchor()` function, menu entry with shortcut, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@anchor.py (navigate_to_anchor, _save_dag_position, navigate_back, is_anchor import from link.py)
@menu.py (menu registration pattern with _add_gated_command)
@tests/test_anchor_navigation.py (test patterns for navigation)
</context>

<interfaces>
<!-- Key functions the executor needs to reuse. Extracted from anchor.py. -->

From anchor.py:
```python
def _save_dag_position():
    """Capture the current DAG viewport state into the back-navigation slot."""
    global _back_position
    _back_position = (nuke.zoom(), nuke.center())

def navigate_to_anchor(anchor_node):
    """Zoom the DAG to frame *anchor_node* and its visible-path upstream nodes."""

def navigate_back():
    """Restore the DAG to the position saved before the last Alt+A jump."""
```

From link.py (imported in anchor.py):
```python
def is_anchor(node) -> bool
```

From menu.py:
```python
def _add_gated_command(menu, name, command, shortcut=None):
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add jump_to_selected_anchor function and tests</name>
  <files>anchor.py, tests/test_anchor_navigation.py</files>
  <behavior>
    - Test 1: jump_to_selected_anchor calls _save_dag_position then navigate_to_anchor when a single anchor node is selected
    - Test 2: jump_to_selected_anchor is a silent no-op when no nodes are selected (no error, no navigation)
    - Test 3: jump_to_selected_anchor is a silent no-op when selected node is not an anchor (e.g. a regular Merge node)
    - Test 4: jump_to_selected_anchor is a silent no-op when prefs.plugin_enabled is False
    - Test 5: After jump_to_selected_anchor, navigate_back restores the saved position (integration — verifies _save_dag_position was called before navigate_to_anchor)
  </behavior>
  <action>
    Add `jump_to_selected_anchor()` function to anchor.py, placed after `navigate_back()` and before `navigate_to_backdrop()` (around line 639). The function should:

    1. Early-return if `prefs.plugin_enabled` is False
    2. Call `nuke.selectedNodes()` — if empty list, return silently
    3. Take the first selected node. Check `is_anchor(node)` — if False, return silently
    4. Call `_save_dag_position()` to save current viewport
    5. Call `navigate_to_anchor(node)` to zoom to the anchor's upstream tree

    No popup, no tabtabtab widget, no deferred Qt timer needed (the user already has focus on the DAG panel since they selected a node there).

    Add tests to tests/test_anchor_navigation.py following the existing test patterns in that file — use unittest.TestCase, mock nuke.selectedNodes, mock is_anchor, mock _save_dag_position and navigate_to_anchor to verify call order.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_anchor_navigation.py -x -v -k "jump_to_selected"</automated>
  </verify>
  <done>jump_to_selected_anchor() exists, all 5 test cases pass, function correctly delegates to _save_dag_position and navigate_to_anchor</done>
</task>

<task type="auto">
  <name>Task 2: Add menu entry for jump-to-anchor command</name>
  <files>menu.py</files>
  <action>
    Add a gated menu command for the new function in menu.py. Place it after the "Anchor Find" (Alt+A) line (line 52) and before "Anchor Back" (Alt+Z):

    ```python
    _add_gated_command(anchors_menu, "Anchor Jump", "anchor.jump_to_selected_anchor()", "alt+J")
    ```

    Use Alt+J as the shortcut — it's mnemonic ("Jump"), not already taken, and follows the Alt+key pattern of Alt+A (find) and Alt+Z (back).
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/ -x -v --timeout=30</automated>
  </verify>
  <done>Menu entry registered with Alt+J shortcut, full test suite passes (208+ tests), no ruff violations</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -v` — full suite passes (208+ tests)
- `ruff check *.py` — zero violations on anchor.py and menu.py
- New function `jump_to_selected_anchor` exists in anchor.py
- Menu entry with Alt+J shortcut registered in menu.py
</verification>

<success_criteria>
- jump_to_selected_anchor() navigates to selected anchor's upstream tree without showing a popup
- Current DAG position is saved before jump (navigate_back works to return)
- Command is no-op for non-anchor selections, empty selections, or disabled plugin
- Alt+J shortcut registered in Anchors menu
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260328-6mn-add-jump-to-anchor-command-using-selecte/260328-6mn-SUMMARY.md`
</output>
