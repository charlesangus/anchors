---
phase: quick
plan: 260318-r7o
type: execute
wave: 1
depends_on: []
files_modified:
  - colors.py
  - anchor.py
  - tests/test_anchor_color_system.py
autonomous: true
requirements: [quick-task]

must_haves:
  truths:
    - "When creating an anchor, the color palette dialog shows a labelled swatch indicating the default/auto-computed color"
    - "When renaming an anchor, the color palette dialog shows a labelled swatch indicating the current auto-computed color"
    - "Clicking the default color swatch selects it like any other swatch"
    - "When no default_color is provided (e.g. Set Color flow), no default swatch section appears"
  artifacts:
    - path: "colors.py"
      provides: "ColorPaletteDialog with default_color parameter and default swatch display"
      contains: "default_color"
    - path: "anchor.py"
      provides: "create_anchor and rename_anchor pass default_color to ColorPaletteDialog"
      contains: "default_color"
    - path: "tests/test_anchor_color_system.py"
      provides: "Tests for default color swatch display in ColorPaletteDialog"
      contains: "default_color"
  key_links:
    - from: "anchor.py:create_anchor"
      to: "colors.py:ColorPaletteDialog"
      via: "default_color parameter passed with find_anchor_color() result"
      pattern: "default_color.*find_anchor_color"
    - from: "anchor.py:rename_anchor"
      to: "colors.py:ColorPaletteDialog"
      via: "default_color parameter passed with find_anchor_color() result"
      pattern: "default_color.*find_anchor_color"
---

<objective>
Add a "Default Colour" section to the ColorPaletteDialog that shows the auto-computed colour — the colour that would be assigned without any user input. This appears in the create/rename anchor flows so the user can see what the system default is and optionally select it.

Purpose: Users want to see what colour the system would assign automatically, so they can make an informed choice about whether to override it or accept the default.
Output: Updated ColorPaletteDialog with default_color display, updated anchor.py callers, tests.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@colors.py (ColorPaletteDialog — the dialog to modify)
@anchor.py (create_anchor, rename_anchor — the callers that open the dialog)
@constants.py (ANCHOR_DEFAULT_COLOR)
@tests/test_anchor_color_system.py (existing tests)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default_color parameter to ColorPaletteDialog and display default swatch</name>
  <files>colors.py, tests/test_anchor_color_system.py</files>
  <behavior>
    - Test: When default_color is provided, a QLabel "Default Colour" appears in the dialog layout
    - Test: When default_color is provided, a swatch button with that color is rendered below the label
    - Test: When default_color is None (not provided), no "Default Colour" label or swatch appears
    - Test: Clicking the default color swatch selects it and accepts the dialog (same as clicking any other swatch)
    - Test: The default color swatch has a 2px border using the highlight color when it matches the initial_color (pre-selected)
  </behavior>
  <action>
Add a `default_color` parameter (int or None, default None) to `ColorPaletteDialog.__init__`.

In `_build_ui`, after the name field and BEFORE the swatch grid:
- If `default_color is not None`, add a section with:
  - A QLabel "Default Colour" (using British spelling to match the user's description)
  - A single 24x24 swatch button showing the default_color, styled like other swatches
  - Clicking it sets `self._selected_color = default_color` and calls `self.accept()`
  - Store as `self._default_color_button` for border refresh
  - The default color swatch should participate in `_refresh_swatch_borders()` — if `self._selected_color == default_color`, it gets the highlight border

Store `self._default_color = default_color` on the instance so `_refresh_swatch_borders` can access it.

In `_refresh_swatch_borders`, after the existing loop over `self._swatch_cells`, also update the default color button's border if it exists.

Write tests in a new test class `TestColorPaletteDialogDefaultColorSwatch` in `tests/test_anchor_color_system.py`. Use the same pattern as existing tests — mock nuke, create dialog instances, assert widget presence/absence and behavior.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_anchor_color_system.py -x -q -k "DefaultColor" 2>&1 | tail -20</automated>
  </verify>
  <done>ColorPaletteDialog accepts default_color param, shows labelled default swatch when provided, hides it when not provided, clicking it selects and accepts. All new tests pass, all existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Pass default_color from create_anchor and rename_anchor to ColorPaletteDialog</name>
  <files>anchor.py</files>
  <action>
In `create_anchor()` (line ~371): The `pre_color` variable already holds `find_anchor_color(input_node)` or `ANCHOR_DEFAULT_COLOR`. Pass it as `default_color=int(pre_color)` to `ColorPaletteDialog()`.

In `rename_anchor()` (line ~307): Compute the auto-derived color using `find_anchor_color(anchor_node)` and pass it as `default_color` to the `ColorPaletteDialog()` constructor. This is the colour the system would pick if the user had no opinion — distinct from `current_color` which is what the anchor currently has (which may have been manually set).

Do NOT change `set_anchor_color()` — that flow is for explicitly picking a new color on an existing anchor, and showing a "default" there would be misleading.

The dialog constructor calls should look like:
```python
dialog = ColorPaletteDialog(
    initial_color=...,
    show_name_field=True,
    initial_name=...,
    custom_colors=prefs.custom_colors,
    default_color=int(pre_color),  # new param
)
```
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_anchor_color_system.py tests/test_anchor_naming.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>create_anchor passes find_anchor_color result as default_color to dialog. rename_anchor passes find_anchor_color result as default_color to dialog. set_anchor_color does NOT pass default_color. All tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` — full test suite passes
- Manual verification: In Nuke, create an anchor from a selected node. The color palette dialog shows a "Default Colour" section at the top with a swatch showing the auto-computed color.
</verification>

<success_criteria>
- ColorPaletteDialog shows a "Default Colour" labelled swatch when default_color parameter is provided
- The default swatch is clickable and selects that color
- create_anchor and rename_anchor pass the auto-computed color as default_color
- set_anchor_color does NOT show a default color section
- All existing tests continue to pass
- New tests cover the default color swatch feature
</success_criteria>

<output>
After completion, create `.planning/quick/260318-r7o-in-the-anchor-create-rename-popup-add-a-/260318-r7o-SUMMARY.md`
</output>
